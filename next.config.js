/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["cdn-icons-png.flaticon.com", "img.icons8.com, img2.freepng.es"],
  },
};

module.exports = nextConfig;
